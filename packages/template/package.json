{
  "name": "{{APP_CLI}}",
  "version": "0.1.0",
  "description": "CLI for {{APP_NAME}} API",
  "type": "module",
  "bin": {
    "{{APP_CLI}}": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/index.js --target bun",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
